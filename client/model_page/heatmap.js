function showLinks() {
    const links = document.querySelector(".links")
    links.classList.remove("hide")
}
function hideLinks() {
    const links = document.querySelector(".links")
    links.classList.add("hide")
}
